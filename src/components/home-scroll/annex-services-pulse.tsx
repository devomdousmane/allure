"use client";

/**
 * Disabled — scaling the SplitType target fought the char reveal
 * and caused a visible offset on the price. Keep file as no-op
 * so page imports stay stable.
 */
export function AnnexServicesPulse() {
  return null;
}
